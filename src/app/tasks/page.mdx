# Tasks

Every workflow step and standalone agent run creates a task in the queue. The Tasks API lets you submit, monitor, retry, and cancel tasks. {{ className: 'lead' }}

Tasks track execution status, input/output data, token usage, and timing metadata. Use the endpoints below to submit new tasks, check their progress, and manage failed runs.
