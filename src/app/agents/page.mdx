# Agents

Agents are the core building blocks of AgentFlow. Each agent encapsulates a name, instructions, tools, and optional memory. {{ className: 'lead' }}

Agents can be configured with different LLM models, retry policies, and timeouts. Use the endpoints below to manage agents programmatically — create, list, retrieve, update, and delete them as your agentic workflows evolve.
